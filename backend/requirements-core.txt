# Core requirements for ProfileGPT (Python 3.13 compatible)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.25.0
aiofiles>=23.2.0
python-multipart>=0.0.6

# Database and Supabase (we'll install these manually if needed)
# supabase
# psycopg2-binary
# sqlalchemy
# asyncpg

# OpenAI for LLM and embeddings
openai>=1.0.0

# Basic document processing
# PyPDF2
# python-docx
# beautifulsoup4
# markdownify

# Optional: Redis for caching
# redis